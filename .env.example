# Tunnelr Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED: Your domain
# =============================================================================
BASE_DOMAIN=example.com

# =============================================================================
# ROUTING MODE (Choose one)
# =============================================================================
#
# PATH MODE (default - easiest setup):
#   - Tunnel URLs: https://example.com/t/abc123/webhook
#   - Just point your domain to the server, that's it!
#
# SUBDOMAIN MODE (prettier URLs, more setup):
#   - Tunnel URLs: https://abc123.example.com/webhook
#   - Requires wildcard DNS (*.example.com) + wildcard SSL certificate
#
ROUTING_MODE=path

# =============================================================================
# SSL Configuration
# =============================================================================
# Email for Let's Encrypt certificate notifications
SSL_EMAIL=admin@example.com

# =============================================================================
# Quick Setup Guide
# =============================================================================
#
# 1. Point your domain to your server:
#    - Add A record: example.com -> YOUR_SERVER_IP
#    - If using subdomain mode, also add: *.example.com -> YOUR_SERVER_IP
#
# 2. Set the values above
#
# 3. Start the server:
#    docker compose up -d
#
# 4. Check if everything is working:
#    curl https://example.com/status
#
# 5. Connect from your local machine:
#    tunnelr connect 3000
#
